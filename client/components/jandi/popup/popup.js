/**
 * @fileoverview 프리로더
 * @author young.park <young.park@tosslab.com>
 */

(function() {
  'use strict';

  angular
    .module('jandi.popup', [])
    .config(config)
    .run(run);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }
})();
