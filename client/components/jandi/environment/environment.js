/**
 * @fileoverview 특정 service 또는 검증에 의해 설정된 환경 변수를 제공함
 */
(function() {
  'use strict';

  angular
    .module('jandi.environment', [
      'jandi.browser'
    ])
    .config(config)
    .run(run);

  function config() {

  }

  function run() {

  }

})();
