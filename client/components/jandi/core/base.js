/**
 * @fileoverview base
 * @author young.park <young.park@tosslab.com>
 */

(function() {
  'use strict';

  angular
    .module('jandi.base', [])
    .config(config)
    .run(run);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }
})();
