/**
 * @fileoverview profile image
 *
 */
(function() {
  'use strict';

  angular
    .module('jandi.ui.profileImage', [
      'ui.bootstrap',
      'jandi.core'
    ])
    .run(run)
    .config(config);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }

})();
