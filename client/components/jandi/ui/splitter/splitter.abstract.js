/**
 * @fileoverview splitter
 *
 */
(function() {
  'use strict';

  angular
    .module('jandi.ui.splitter', [
    ])
    .run(run)
    .config(config);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }

})();
