/**
 * @fileoverview splitter
 *
 */
(function() {
  'use strict';

  angular
    .module('jandi.ui.component.splitter', [
    ])
    .run(run)
    .config(config);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }

})();
