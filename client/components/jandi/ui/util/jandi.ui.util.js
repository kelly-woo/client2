/**
 * @fileoverview ui 에 사용될 utility 를 모아놓은 모듈
 *
 */
(function() {
  'use strict';

  angular
    .module('jandi.ui.util', [
    ])
    .run(run)
    .config(config);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }

})();
