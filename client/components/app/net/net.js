/**
 * @fileoverview 네트워크 Interceptor
 * @author Young Park <young.park@tosslab.com>
 */

(function() {
  'use strict';

  angular
    .module('app.net', [])
    .config(config)
    .run(run);

  /* @ngInject */
  function config() {
  }

  /* @ngInject */
  function run() {
  }

})();
